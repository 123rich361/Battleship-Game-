#ifndef COORDINATES_H
#define COORDINATES_H

// This a structure to represent a coordinate on the game board and the variables 
// are used to indicate the row and column of the board  .
struct Coordinates {
    
    int row; 
    int col; 
};

#endif